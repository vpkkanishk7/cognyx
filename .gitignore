# Dependencies
node_modules/
__pycache__/
*.pyc
*.pyo
.env
.env.local
.env.production

# Database (SQLite — contains user data, don't commit)
backend/*.sqlite
backend/*.db
*.sqlite
*.db

# Uploads (session videos, clock drawings, etc.)
uploads/
backend/uploads/

# Large binary / debug scratch files
b64.txt
decoded.zip
decoded_zip/
debug_screenshot.png
test_render.png
full_script_dump.txt
dementia_model.joblib

# ML model (regenerated by running train_model.py)
ml_service/dementia_model.joblib

# Build artifacts / temp files
*.log
*.tmp

# OS files
.DS_Store
Thumbs.db
